---
name: frontend-design
description: Use when Baron routes UI, page, component, layout, responsive, accessibility, interaction, or browser-facing product flow work.
---

# Frontend Design Blueprint

Bundled optional domain skill.

This skill should be lazy-loaded only when a task matches frontend or UI work.
It must not become a workflow core. It should remain compatible with Baron
context compiler output and adapter-specific instructions.
