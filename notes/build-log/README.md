# Baron Build Log

This folder is temporary working memory for building Baron.

Use it to avoid losing context if a session is interrupted, quota ends, or the
machine disconnects. It is safe to delete after Baron reaches a mature release.

Durable product truth belongs under `docs/`, not only in this folder.
